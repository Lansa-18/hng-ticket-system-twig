[phases.setup]
nixPkgs = [
  "php81",
  "php81Extensions.json",
  "nodejs_18",
  "npm"
]

[phases.install]
cmds = [
  "composer install --no-dev",
  "npm install"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "vendor/bin/heroku-php-apache2 public/"
